/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname:'viralburgers.pockethost.io'
            }
        ]
    }
};

export default nextConfig;
